"""Cropping utilities for extracting the main subject from an image.

This module will be responsible for identifying and isolating the relevant
sprite region from an uploaded or generated image.
"""

from __future__ import annotations

from typing import Any


def crop_subject(image: Any) -> Any:
    """Crop the main subject from an image.

    Future implementation will detect the primary character or object region and
    return the cropped image for later animation or sheet processing.

    Args:
        image (Any): The input image to crop.

    Returns:
        Any: The cropped image containing the main subject.
    """
    pass
